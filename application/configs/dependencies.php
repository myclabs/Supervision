<?php

/**
 * Définir ici les dépendances du projet. On indiquera donc quels sont les packages
 * de la librairie que l'on souhaite utiliser avec le numéro de version.
 *
 * Format : 'NomPackage' => 'NumeroVersion'
 *
 * Si NumeroVersion est égal à 'trunk' alors c'est la version du trunk qui est prise,
 * autrement, l'algo regardera dans le répertoire 'tags' puis dans le répertoire 'branches'
 * pour trouver la version indiquée.
 */
$dependencies = array(
    'Core' => 'trunk',
    'UI'   => 'trunk',
);


/**
 * Définir ici les ressources externes à utiliser (tel que Zend, ZFDebug, ...)
 */

